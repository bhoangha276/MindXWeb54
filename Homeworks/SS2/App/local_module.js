// const functions = require("./core_module");

// functions.addUser({ username: 'Hoang Ha', password: '123' });

// let users = functions.readUsers();
// users.forEach(user => {
//     console.log(`${user.id} - ${user.username} - ${user.password}`);
// });

// functions.readUser(2);
// console.log(`${user.id} - ${user.username} - ${user.password}`);


// functions.deleteUser(15);